import {Router} from 'express'; import Customer from '../models/Customer.js'; import {requireAuth} from '../middleware/auth.js'; const router=Router();router.use(requireAuth);router.get('/',async(req,res,next)=>{try{res.json(await Customer.find().sort({updatedAt:-1}).limit(100))}catch(e){next(e)}});router.get('/:id',async(req,res,next)=>{try{const item=await Customer.findById(req.params.id);if(!item)return res.status(404).json({error:{message:'Customer not found.'}});res.json(item)}catch(e){next(e)}});router.patch('/:id',async(req,res,next)=>{try{const item=await Customer.findByIdAndUpdate(req.params.id,req.body,{new:true,runValidators:true});if(!item)return res.status(404).json({error:{message:'Customer not found.'}});res.json(item)}catch(e){next(e)}});export default router;
