import {Navigate,Route,Routes} from 'react-router-dom';import AppLayout from './layouts/AppLayout';import Conversations from './pages/Conversations';import HumanQueue from './pages/HumanQueue';import Login from './pages/Login';import Settings from './pages/Settings';import SystemStatus from './pages/SystemStatus';import WhatsAppSetup from './pages/WhatsAppSetup';
const Protected=({children})=>sessionStorage.getItem('customer_ai_token')?children:<Navigate to="/login" replace/>;
export default function App(){return <Routes><Route path="/login" element={<Login/>}/><Route element={<Protected><AppLayout/></Protected>}><Route path="/inbox" element={<Conversations/>}/><Route path="/conversations/:id" element={<Conversations/>}/><Route path="/settings" element={<Settings/>}/><Route path="/system-status" element={<SystemStatus/>}/><Route path="/human-required" element={<HumanQueue/>}/><Route path="/whatsapp-setup" element={<WhatsAppSetup/>}/></Route><Route path="/" element={<Navigate to="/inbox" replace/>}/><Route path="*" element={<Navigate to="/inbox" replace/>}/></Routes>}
